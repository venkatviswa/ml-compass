{
  "name": "ml-compass-mcp-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "ML Compass as a remote MCP server on Cloudflare Workers (profile-in, no raw data).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "agents": "^0.17.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
